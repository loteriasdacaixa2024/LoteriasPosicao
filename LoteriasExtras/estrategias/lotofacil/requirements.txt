flask
pandas
openpyxl
numpy
# sqlite3 is part of the standard library, but I'll list it if explicitly asked for documentation purposes
# though normally it shouldn't be in requirements.txt
python-dotenv
